{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "eks-kubeconfig-rotator.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "eks-kubeconfig-rotator.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
