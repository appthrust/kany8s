apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eks-kubeconfig-rotator.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "eks-kubeconfig-rotator.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "eks-kubeconfig-rotator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "eks-kubeconfig-rotator.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "eks-kubeconfig-rotator.serviceAccountName" . }}
      {{- include "eks-kubeconfig-rotator.imagePullSecrets" . | nindent 6 }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      containers:
        - name: manager
          image: {{ include "eks-kubeconfig-rotator.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /eks-kubeconfig-rotator
          args:
            {{- include "eks-kubeconfig-rotator.args" . | nindent 12 }}
          env:
            {{- if eq .Values.aws.mode "staticSecret" }}
            - name: AWS_SHARED_CREDENTIALS_FILE
              value: {{ .Values.aws.staticSecret.envFilePath | quote }}
            {{- end }}
            {{- if .Values.aws.region }}
            - name: AWS_REGION
              value: {{ .Values.aws.region | quote }}
            - name: AWS_DEFAULT_REGION
              value: {{ .Values.aws.region | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if eq .Values.aws.mode "staticSecret" }}
          volumeMounts:
            - name: aws-creds
              mountPath: {{ .Values.aws.staticSecret.mountPath }}
              readOnly: true
          {{- end }}
      {{- if eq .Values.aws.mode "staticSecret" }}
      volumes:
        - name: aws-creds
          secret:
            secretName: {{ .Values.aws.staticSecret.secretName }}
            optional: {{ .Values.aws.staticSecret.optional }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
