---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: kany8scontrolplanes.controlplane.cluster.x-k8s.io
spec:
  group: controlplane.cluster.x-k8s.io
  names:
    kind: Kany8sControlPlane
    listKind: Kany8sControlPlaneList
    plural: kany8scontrolplanes
    singular: kany8scontrolplane
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Ready condition status
      jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: READY
      type: string
    - description: Control plane initialized
      jsonPath: .status.initialization.controlPlaneInitialized
      name: INITIALIZED
      type: boolean
    - description: Control plane endpoint host
      jsonPath: .spec.controlPlaneEndpoint.host
      name: ENDPOINT
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Kany8sControlPlane is the Schema for the kany8scontrolplanes
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Kany8sControlPlaneSpec defines the desired state of Kany8sControlPlane.
            properties:
              controlPlaneEndpoint:
                description: ControlPlaneEndpoint is set by the controller once the
                  endpoint becomes available.
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    type: integer
                type: object
              kroSpec:
                description: KroSpec contains arbitrary provider-specific inputs passed
                  to the kro instance.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              resourceGraphDefinitionRef:
                description: ResourceGraphDefinitionRef references the kro ResourceGraphDefinition
                  that materializes this control plane.
                properties:
                  name:
                    description: Name is the name of the referenced ResourceGraphDefinition.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              version:
                description: Version is the Kubernetes version for the control plane.
                minLength: 1
                type: string
            required:
            - resourceGraphDefinitionRef
            - version
            type: object
          status:
            description: Kany8sControlPlaneStatus defines the observed state of Kany8sControlPlane.
            properties:
              conditions:
                description: Conditions defines current service state of the Kany8sControlPlane.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                description: FailureMessage indicates that there is a fatal problem
                  reconciling the control plane.
                type: string
              failureReason:
                description: FailureReason indicates that there is a fatal problem
                  reconciling the control plane.
                type: string
              initialization:
                description: Initialization tracks whether the control plane has been
                  initialized.
                properties:
                  controlPlaneInitialized:
                    description: ControlPlaneInitialized indicates that the control
                      plane has been initialized.
                    type: boolean
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
