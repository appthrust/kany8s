# Ralphy Progress Log


- 2026-01-25: Initialized Go module + kubebuilder scaffolding; verified `make test` and `make lint`.

- 2026-01-25: Pinned Go toolchain + tracked/pinned dev tools (controller-gen, kustomize, setup-envtest, golangci-lint) in go.mod; added hack/tools.go and a unit test to enforce it; verified `make test` and `make lint`.

- 2026-01-25: Documented local development prerequisites/quickstart in README; added a unit test to enforce presence; verified `make test` and `make lint`.

- 2026-01-25: Pinned Dockerfile Go builder image to the go.mod toolchain; added a unit test to enforce it; verified `make test`, `make lint`, and `make docker-build IMG=example.com/kany8s/controller:dev`.

- 2026-01-25: Added GitHub Actions CI workflow to run `make test`, `make manifests`, and `make lint`; added a unit test to enforce it; verified `make test` and `make lint`.

- 2026-01-25: Scaffolded `Kany8sControlPlane` API (kubebuilder create api); added a unit test enforcing the scaffold exists; verified `make test` and `make lint`.

- 2026-01-25: Defined `Kany8sControlPlaneSpec` MVP fields (version, resourceGraphDefinitionRef, kroSpec passthrough, controlPlaneEndpoint); added unit tests; verified `make test` and `make lint`.

- 2026-01-25: Defined `Kany8sControlPlaneStatus` MVP fields (initialization.controlPlaneInitialized, conditions accessors, failureReason/failureMessage); added unit tests; verified `make test` and `make lint`.

- 2026-01-25: Scaffolded `Kany8sControlPlane` controller (kubebuilder create api --controller --resource=false); added a unit test enforcing the scaffold exists and is wired into the manager; verified `make test` and `make lint`.

- 2026-01-25: Added `kubectl get` print columns (INITIALIZED/ENDPOINT) to `Kany8sControlPlane`; added a unit test enforcing the markers; verified `make test` and `make lint`.

- 2026-01-25: Ran `make generate` + `make manifests` to confirm `config/crd/bases/` is up to date; added a devtools unit test to enforce key CRD YAML contents; verified `make test` and `make lint`.

- 2026-01-25: Implemented kro `ResourceGraphDefinition` schema->instance GVK resolver (`internal/kro/ResolveInstanceGVK`) with table-driven unit tests; verified `make test` and `make lint`.

- 2026-01-25: Implemented `Kany8sControlPlane` reconcile to resolve kro instance GVK and create/update the corresponding unstructured kro instance; added a unit test for instance creation; verified `make test` and `make lint`.

- 2026-01-25: Implemented kro instance `.spec` rendering from `Kany8sControlPlane.spec.kroSpec` with enforced `.spec.version`; added unit tests covering drift correction; verified `make test` and `make lint`.

- 2026-01-25: Set controller OwnerReference on kro instances (GC on Kany8sControlPlane delete); added unit test; verified `make test` and `make lint`.

- 2026-01-25: Standardized missing/invalid kro ResourceGraphDefinition handling (Condition + Warning Event + RequeueAfter) in `Kany8sControlPlane` reconcile; added unit tests; verified `make test` and `make lint`.

- 2026-01-25: Added safe kro instance status reader helper (`internal/kro/status.go`) for normalized `ready/endpoint/reason/message`, with unit tests; verified `make test` and `make lint`.

- 2026-01-25: Added endpoint string parser utility (`internal/endpoint/parse.go`) with table-driven unit tests; verified `make test` and `make lint`.

- 2026-01-25: Reflected kro instance `status.endpoint` into `Kany8sControlPlane.spec.controlPlaneEndpoint`; added unit test; verified `make test` and `make lint`.

- 2026-01-25: Set `Kany8sControlPlane.status.initialization.controlPlaneInitialized=true` once an endpoint is observed; added unit test to ensure it is sticky; verified `make test` and `make lint`.

- 2026-01-25: Updated `Kany8sControlPlane` reconcile to set Creating/Ready conditions and propagate kro `reason/message` into `failureReason/failureMessage`; added unit tests; verified `make test` and `make lint`.

- 2026-01-25: Added a fixed 15s poll interval while the control plane is not Ready (centralized as `internal/constants/ControlPlaneNotReadyRequeueAfter`); added unit test; verified `make test` and `make lint`.

- 2026-01-25: Added a unit test covering the end-to-end polling flow (requeue while not Ready, then stop once Ready+endpoint) and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-25: Added manager RBAC to read kro `ResourceGraphDefinition` objects via kubebuilder RBAC markers; regenerated manifests and added a devtools test enforcing the generated `config/rbac/role.yaml`; verified `make test` and `make lint`.

- 2026-01-25: Added broad manager RBAC for dynamically generated kro instances (`kro.run` resources `*` with create/get/list/watch/update/patch); regenerated manifests and updated the devtools RBAC test; verified `make test` and `make lint`.

- 2026-01-25: Added manager RBAC for creating/patching Events (core + `events.k8s.io`) and a devtools test enforcing the generated `config/rbac/role.yaml`; verified `make test` and `make lint`.

- 2026-01-25: Implemented dynamic watch for kro instances (dynamic informer + channel source) so instance updates enqueue `Kany8sControlPlane` reconciles; added an envtest covering update-triggered reconcile; verified `make test` and `make lint`.

- 2026-01-25: Added `examples/capi/cluster.yaml` minimal `Cluster` + `Kany8sControlPlane` example and a devtools unit test enforcing it; verified `make test` and `make lint`.

- 2026-01-25: Added a devtools test enforcing generated manager RBAC for `Kany8sControlPlane` (CRUD + status/finalizers); verified `make test` and `make lint`.

- 2026-01-25: Ensured `make manifests` explicitly generates RBAC into `config/rbac/` (controller-gen output config); added a devtools unit test enforcing it; verified `make test` and `make lint`.

- 2026-01-25: Expanded endpoint parser unit tests (query/fragment/userinfo + trailing slash coverage) and marked the PRD test item complete; verified `make test` and `make lint`.

- 2026-01-25: Added a minimal kro RGD + instance example for the normalized `status.ready`/`status.endpoint` contract under `examples/kro/`; added a devtools test to enforce its presence; verified `make test` and `make lint`.

- 2026-01-25: Expanded `ResolveInstanceGVK` unit tests (success + error cases) and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-25: Documented a kind+kro demo flow in README (install -> apply RGD -> apply Cluster/Kany8sControlPlane); updated the CAPI sample to use the demo RGD; added a devtools test to enforce the README section; verified `make test` and `make lint`.

- 2026-01-25: Added a fake-client unit test for the "Ready but endpoint missing" reconcile case (requeue + no endpoint/initialized) and marked the PRD fake-client reconcile test task complete; verified `make test` and `make lint`.

- 2026-01-25: Marked the PRD CI `make test` task complete (CI workflow + devtools assertion already present); verified `make test` and `make lint`.

- 2026-01-25: Added an initial ACK-based EKS control plane kro RGD example (`examples/kro/eks/eks-control-plane-rgd.yaml`) plus a devtools test and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-25: Projected the EKS RGD instance `status.endpoint` from the ACK EKS Cluster status in `examples/kro/eks/eks-control-plane-rgd.yaml`; updated the devtools assertion; verified `make test` and `make lint`.

- 2026-01-25: Materialized the EKS RGD instance `status.ready` as a boolean using the `int(...) == 1` pattern (kro v0.7.1 bool-missing workaround); updated devtools sample assertions and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-25: Updated the EKS kro RGD to set `Cluster.spec.roleARN` from `Role.status.ackResourceMetadata.arn` (DAG dependency) and removed the temporary `awsAccountID` input; updated devtools assertions; verified `make test` and `make lint`.

- 2026-01-25: Set the ACK EKS `Cluster` resource `readyWhen` in `examples/kro/eks/eks-control-plane-rgd.yaml` to wait for `ACTIVE` + non-empty endpoint (self-only readyWhen constraint); updated the devtools assertion; verified `make test` and `make lint`.

- 2026-01-25: Added a kind+kro setup runbook (`docs/runbooks/kind-kro.md`) plus a devtools test enforcing it; verified `make test` and `make lint`.

- 2026-01-25: Added an ACK-based EKS addons kro RGD example (`examples/kro/eks/eks-addons-rgd.yaml`) plus a devtools test and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-25: Added an EKS PodIdentitySet kro RGD example (`examples/kro/eks/pod-identity-set-rgd.yaml`) that ensures Role -> PodIdentityAssociation DAG ordering; added a devtools test and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-25: Added a chained EKS platform cluster kro RGD sample (`examples/kro/eks/platform-cluster-rgd.yaml`) that composes EKSControlPlane + EKSAddons + PodIdentitySet while projecting control plane `status.ready`/`status.endpoint`; added a devtools test and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-25: Added an ACK installation/authentication runbook (`docs/runbooks/ack.md`) plus a devtools test enforcing it; verified `make test` and `make lint`.

- 2026-01-25: Added an e2e troubleshooting command runbook (`docs/runbooks/e2e.md`) plus a devtools test enforcing it; verified `make test` and `make lint`.

- 2026-01-25: Defined a provider-agnostic kubeconfig retrieval contract (`status.kubeconfigSecretRef`) in `docs/design.md`; added a devtools test enforcing the contract is documented; verified `make test` and `make lint`.

- 2026-01-25: Documented Option A requirements for having the RGD create the CAPI-compatible `<cluster>-kubeconfig` Secret in `docs/design.md`; added a devtools test enforcing the section; verified `make test` and `make lint`.

- 2026-01-25: Documented Option B input contract for having Kany8s create the CAPI-compatible `<cluster>-kubeconfig` Secret from a provider-specific source Secret; added a devtools test enforcing the design section; verified `make test` and `make lint`.

- 2026-01-25: Added CAPI-compatible kubeconfig Secret contract helpers (`internal/kubeconfig`) for `<cluster>-kubeconfig` (type/label/data key) with unit tests; verified `make test` and `make lint`.

- 2026-01-25: Implemented kubeconfig Secret reconciliation in the `Kany8sControlPlane` controller (copy from kro instance `status.kubeconfigSecretRef` into CAPI `<cluster>-kubeconfig` Secret), added unit tests + secrets RBAC devtools check; verified `make test` and `make lint`.

- 2026-01-25: Added ClusterClass template APIs (`Kany8sControlPlaneTemplate`, `Kany8sClusterTemplate`) with minimal template resource specs; added devtools tests asserting scaffold + generated CRD bases; verified `make test` and `make lint`.

- 2026-01-25: Documented ClusterClass/Topology version single source of truth (`Cluster.spec.topology.version` -> `Kany8sControlPlane.spec.version`) in `docs/design.md`; added a devtools assertion test; verified `make test` and `make lint`.

- 2026-01-25: Added a ClusterClass + template sample showing variables/patches mapping into `Kany8sControlPlaneTemplate.spec.template.spec.kroSpec` (`examples/capi/clusterclass.yaml`); added a devtools test enforcing it; verified `make test` and `make lint`.

- 2026-01-25: Documented the MVP `clusterctl` packaging approach (using `make build-installer` output as the `ControlPlaneProvider` components manifest) in `docs/runbooks/clusterctl.md`; added a devtools test enforcing the runbook exists; verified `make test` and `make lint`.

- 2026-01-26: Enabled ClusterClass/Topology upgrades for `Kany8sControlPlane` by adding required CAPI contract labels to the generated CRD and implementing `status.version` (set once Ready); added a unit test verifying a topology version change propagates to the kro instance; verified `make test` and `make lint`.

- 2026-01-26: Validated `config/default` is ready for `make deploy` by adding a devtools test enforcing kustomize namespace/prefix/resources/metrics patch; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-26: Decided distribution packaging to use the clusterctl `ControlPlaneProvider` components bundle (`dist/install.yaml`) and explicitly documented that Kany8s does not ship a Helm chart; updated the devtools runbook assertion; verified `make test` and `make lint`.

- 2026-01-26: Documented Kany8s versioning + release flow (tags, release notes, image publishing, bundle generation) in `docs/release.md`; added a devtools test enforcing the runbook exists and includes the key commands; verified `make test` and `make lint`.

- 2026-01-26: Documented the normalized RGD status contract in `docs/rgd-contract.md`; added a devtools test enforcing key contract fields; verified `make test` and `make lint`.

- 2026-01-26: Added stub GKE/AKS control plane kro RGD templates under `examples/kro/` with a devtools test; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-26: Documented kro RGD authoring guidelines (static analysis + v0.7.1 pitfalls like `NetworkPolicy`, status CEL quirks) in `docs/rgd-guidelines.md`; added a devtools test enforcing key guidance; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-26: Enabled kubebuilder multi-group support and scaffolded the `Kany8sCluster` infrastructure API; defined a minimal `Kany8sCluster` CRD contract (`spec.kroSpec`, `status.conditions`, failure reason/message) with CAPI v1beta2 contract label; added devtools tests + generated CRD/RBAC manifests; verified `make test` and `make lint`.

- 2026-01-26: Implemented `Kany8sCluster` infrastructure controller to set the Ready condition True (unblocking CAPI `InfrastructureReady`); added a unit test; verified `make test` and `make lint`.

- 2026-01-28: Implemented CAPI v1beta2 InfrastructureCluster `status.initialization.provisioned` for `Kany8sCluster` and set it in the infra controller; updated unit + devtools tests; regenerated manifests/deepcopy; verified `make test` and `make lint`.

- 2026-01-28: Scaffolded infrastructure `Kany8sClusterTemplate` API via kubebuilder; added devtools tests asserting the scaffold + generated CRD base; regenerated manifests; verified `make test` and `make lint`.

- 2026-01-28: Aligned infrastructure `Kany8sClusterTemplate` to ClusterClass template conventions (spec.template with metadata + kroSpec); extended devtools assertions; regenerated manifests/deepcopy; verified `make test` and `make lint`.

- 2026-01-28: Updated the ClusterClass example to reference the infrastructure-group `Kany8sClusterTemplate`; tightened the devtools example assertion; verified `make test` and `make lint`.

- 2026-01-28: Deprecated the controlplane-group `Kany8sClusterTemplate` in `docs/design.md` (use the infrastructure-group template going forward); added a devtools test enforcing the note; verified `make test` and `make lint`.

- 2026-01-28: Removed the deprecated controlplane-group `Kany8sClusterTemplate` API (types, generated CRD base, samples, and PROJECT entry); updated docs/tests; regenerated manifests/deepcopy; verified `make test` and `make lint`.

- 2026-01-28: Made invalid `Kany8sControlPlane.spec.kroSpec` non-fatal (surface via Conditions + Warning Event) and added unit test coverage; verified `make test` and `make lint`.

- 2026-01-28: Clarified `spec.kroSpec` expectations in `docs/design.md` (must be a JSON object because the controller injects `spec.version`); added a devtools test; verified `make test` and `make lint`.

- 2026-01-28: Reserved `Kany8sControlPlane.status.failureReason/failureMessage` for terminal/controller-detected errors (cleared during normal provisioning); updated controller logic, unit tests, and `docs/rgd-contract.md`; verified `make test` and `make lint`.

- 2026-01-28: Clarified the RGD contract required-vs-runtime behavior (RGD authors must provide fields; controller tolerates missing fields) in `docs/rgd-contract.md`; extended devtools coverage and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-28: Documented kro dynamic RBAC rationale and a future tightening approach in `docs/design.md`; added a devtools test enforcing key terms; verified `make test` and `make lint`.

- 2026-01-28: Replaced kubebuilder `config/samples/*` TODO placeholders with minimal working sample manifests; added a devtools test; verified `make test` and `make lint`.

- 2026-01-28: Treated missing kubeconfig source Secret as a wait (RequeueAfter + nil error) and surfaced source Secret get failures via a dedicated Condition + Warning Event; added unit tests; verified `make test` and `make lint`.

- 2026-01-28: Validated kubeconfig bytes before copying into the CAPI `<cluster>-kubeconfig` Secret; reject invalid kubeconfig via Condition + Warning Event and avoid overwriting the target secret; added unit tests; verified `make test` and `make lint`.

- 2026-01-28: Prevented silent dynamicwatch event loss on a full channel by coalescing + flushing pending events and exposing a Prometheus counter for backpressure; added unit tests; verified `make test` and `make lint`.

- 2026-01-28: Retried dynamicwatch setup when `InstanceWatcher.EnsureWatch` fails by requeueing even after the control plane is Ready; added unit test coverage; verified `make test` and `make lint`.

- 2026-01-28: Prevented leaking kro instance endpoints in logs/conditions by omitting raw endpoints from controller log fields/messages and sanitizing `endpoint.Parse` URL parse errors; added unit tests; verified `make test` and `make lint`.

- 2026-01-28: Tightened manager RBAC for core Secrets by removing unnecessary `list`/`watch` verbs; updated RBAC devtools assertions and regenerated manifests; verified `make test` and `make lint`.

- 2026-01-28: Cleaned up kubebuilder-scaffold TODO comments and replaced the placeholder infra controller Ginkgo test with an end-to-end envtest assertion for provisioned+Ready status; verified `make test` and `make lint`.

- 2026-01-28: Updated `issues.md` with follow-up notes for false positives and corrected controller-runtime requeue semantics; added a devtools test enforcing the notes; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-29: Disabled controller-runtime cache for core Secrets to allow kubeconfig Secret reconciliation without `secrets` list/watch RBAC; added a devtools test asserting the manager cache configuration; verified `make test` and `make lint`.

- 2026-01-29: Treated missing kubeconfig source Secret `data["value"]` as a wait (Condition + Warning Event + RequeueAfter) instead of a hard error; added unit test coverage; verified `make test` and `make lint`.

- 2026-01-29: Kept `KubeconfigSecretReconciled` fresh while waiting for a missing kubeconfig source Secret (set Condition=False on NotFound; emit events only on change); added unit test coverage; verified `make test` and `make lint`.

- 2026-01-29: Gated `Kany8sControlPlane` `Ready=True` on kubeconfig Secret reconciliation when `status.kubeconfigSecretRef` is set (surface waits via Ready=False/Creating=True + `KubeconfigSecretReconciled=False`); documented semantics in `docs/rgd-contract.md` and added unit tests; verified `make test` and `make lint`.

- 2026-01-29: Documented self-managed (kubeadm) responsibilities/boundaries (endpoint/kubeconfig/certs source of truth) in `docs/design.md`; added devtools coverage; aligned devtools README/issues doc checks to `docs/`; verified `make test` and `make lint`.

- 2026-01-29: Scaffolded `Kany8sKubeadmControlPlane` API (kubebuilder create api), updated devtools/sample assertions, and regenerated manifests/deepcopy; verified `make test` and `make lint`.

- 2026-01-29: Defined `Kany8sKubeadmControlPlaneSpec` MVP fields (version, replicas default=1, machineTemplate.infrastructureRef, kubeadmConfigSpec, controlPlaneEndpoint); updated sample + devtools assertions and regenerated manifests/deepcopy; verified `make test` and `make lint`.

- 2026-01-29: Defined `Kany8sKubeadmControlPlaneStatus` (version, initialization.controlPlaneInitialized, conditions accessors, failureReason/failureMessage); added devtools assertions; verified `make test` and `make lint`.

- 2026-01-29: Confirmed `make generate` + `make manifests` produce no diff and the generated `Kany8sKubeadmControlPlane` CRD base is up to date; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-29: Scaffolded the `Kany8sKubeadmControlPlane` controller (kubebuilder) and wired it into the manager; added a devtools scaffold assertion; verified `make test` and `make lint`.

- 2026-01-29: Implemented `Kany8sKubeadmControlPlane` owner `Cluster` resolution via `util.GetOwnerCluster` (wait/requeue with a surfaced Condition when missing); added unit tests; verified `make test` and `make lint`.

- 2026-01-29: Implemented infra cluster endpoint resolution for `Kany8sKubeadmControlPlane` by reading the referenced infra cluster `spec.controlPlaneEndpoint` via Cluster API contract-versioned external get; added unit tests; verified `make test` and `make lint`.

- 2026-01-29: Implemented self-managed cluster certificate Secret generation for `Kany8sKubeadmControlPlane` using Cluster API `util/secret` (`NewCertificatesForInitialControlPlane` + `LookupOrGenerate`); added unit tests covering generated `<cluster>-ca/etcd/proxy/sa` Secrets; verified `make test` and `make lint`.

- 2026-01-29: Implemented self-managed `<cluster>-kubeconfig` Secret generation for `Kany8sKubeadmControlPlane` from the Cluster CA Secret (Cluster API `util/kubeconfig`); added unit tests and verified `make test` and `make lint`.

- 2026-01-29: Implemented infra machine template cloning for `Kany8sKubeadmControlPlane` (create a control-plane infraMachine from `spec.machineTemplate.infrastructureRef` via Cluster API `external.GenerateTemplate`); added unit tests and verified `make test` and `make lint`.

- 2026-01-29: Implemented initial control plane `KubeadmConfig` reconciliation for `Kany8sKubeadmControlPlane` (sync `clusterConfiguration.controlPlaneEndpoint`, inject cert files); added unit tests and verified `make test` and `make lint`.

- 2026-01-29: Implemented `Kany8sKubeadmControlPlane` reconciliation to create a control plane `Machine` referencing the generated `KubeadmConfig` and infraMachine; added unit tests and verified `make test` and `make lint`.

- 2026-01-29: Implemented self-managed readiness semantics for `Kany8sKubeadmControlPlane` (Initialized when a control plane Machine is `Ready=True`; Ready when endpoint is resolved and Initialized=true), with unit tests; verified `make test` and `make lint`.

- 2026-01-29: Registered Cluster API core + CABPK schemes in the manager (`cmd/main.go`) and added a devtools test to enforce it; verified `make test` and `make lint`.

- 2026-01-29: Added manager RBAC for `Kany8sKubeadmControlPlane` dependencies (Clusters, Machines, KubeadmConfigs, infrastructure `*`); updated RBAC devtools assertions and regenerated manifests; verified `make test` and `make lint`.

- 2026-01-29: Added a fake-client minimal-flow unit test for `Kany8sKubeadmControlPlane` (wait for infra endpoint -> create kubeconfig/KubeadmConfig/Machine/infraMachine); verified `make test` and `make lint`.

- 2026-01-29: Added CAPD+kubeadm self-managed Docker sample manifests under `examples/self-managed-docker/` with a devtools test enforcing key resources; verified `make test` and `make lint`.

- 2026-01-29: Added a CAPD+kubeadm self-managed runbook (`docs/runbooks/capd-kubeadm.md`) plus a devtools test enforcing key commands; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-01-29: Added a CAPD+kubeadm self-managed acceptance test script (`hack/acceptance-test-self-managed.sh`) + Makefile targets; added devtools coverage; verified `make test` and `make lint`.

- 2026-02-02: Documented the infrastructure RGD instance normalized status contract for `Kany8sCluster` in `docs/rgd-contract.md`; extended the devtools doc test; verified `make test` and `make lint`.

- 2026-02-02: Added infrastructure-group `ResourceGraphDefinitionReference` type (for upcoming `spec.resourceGraphDefinitionRef`) plus a devtools test; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-02: Added optional `spec.resourceGraphDefinitionRef` to infrastructure `Kany8sCluster` as the kro integration switch; regenerated manifests/deepcopy and updated devtools assertions; verified `make test` and `make lint`.

- 2026-02-02: Added optional `spec.template.spec.resourceGraphDefinitionRef` to infrastructure `Kany8sClusterTemplate` (Topology switch for kro mode) and updated devtools CRD/schema assertions; verified `make test` and `make lint`.

- 2026-02-02: Updated infrastructure `config/samples` to include kro mode `resourceGraphDefinitionRef` and tightened devtools sample contract checks; verified `make test` and `make lint`.

- 2026-02-02: Added kubebuilder RBAC markers to the infrastructure `Kany8sCluster` controller for kro `ResourceGraphDefinition` reads and dynamic kro instance create/update; added a devtools test asserting the markers exist; regenerated manifests; verified `make test` and `make lint`.

- 2026-02-02: Implemented infrastructure `Kany8sCluster` kro mode scaffolding (RGD -> instance GVK resolution + 1:1 kro instance create/update) in `internal/controller/infrastructure/kany8scluster_controller.go`; added a fake-client unit test in `internal/controller/infrastructure/kany8scluster_reconciler_test.go`; verified `make test` and `make lint`.

- 2026-02-02: Made infrastructure kro instance spec rendering idempotent (expand `Kany8sCluster.spec.kroSpec` + inject `spec.clusterName`/`spec.clusterNamespace`) and added unit tests for drift correction; verified `make test` and `make lint`.

- 2026-02-02: Aligned infrastructure `Kany8sCluster` kro mode status/conditions (provisioned/Ready + terminal failure semantics) to the normalized kro instance status; added fake-client unit tests; verified `make test` and `make lint`.

- 2026-02-02: Added fake-client unit tests for `Kany8sCluster` kro mode to cover missing/invalid ResourceGraphDefinition handling (Ready reason/message + requeue) and updated the reconciler to surface those waits; verified `make test` and `make lint`.

- 2026-02-02: Confirmed devtools regression coverage for infrastructure API/CRD/samples and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-02: Added a minimal infra kro RGD example under `examples/kro/infra/` and updated `examples/capi/cluster.yaml` to include a `Kany8sCluster` in kro mode; added devtools tests to enforce the example files; verified `make test` and `make lint`.

- 2026-02-02: Documented Parent RGD (infra + control plane) status projection guidance in `docs/rgd-contract.md`; extended the devtools doc test; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-02: Documented Approach A guidance for wiring infra outputs into the control plane spec (missing-field tolerance via `.?` + `orValue(...)` and `includeWhen` gating) in `docs/rgd-guidelines.md`; updated devtools assertions; verified `make test` and `make lint`.

- 2026-02-03: Added Makefile target `test-acceptance-kro-infra-reflection` and a devtools unit test to enforce its presence; verified `make test` and `make lint`.

- 2026-02-03: Added Makefile keep target `test-acceptance-kro-infra-reflection-keep` and extended devtools assertions; verified `make test` and `make lint`.

- 2026-02-03: Added acceptance source-of-truth infra kro RGD manifest at `test/acceptance_test/manifests/kro/infra/rgd.yaml` with a devtools test enforcing key contents; verified `make test` and `make lint`.

- 2026-02-03: Added stub `hack/acceptance-test-kro-infra-reflection.sh` plus a devtools test enforcing it exists; verified `make test` and `make lint`.

- 2026-02-03: Added wrapper runner `test/acceptance_test/run-acceptance-kro-infra-reflection.sh` and a devtools test enforcing it delegates to the hack script; verified `make test` and `make lint`.

- 2026-02-03: Fixed the kro infra reflection acceptance RGD name (`demo-infra.kro.run`) as a constant in `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Fixed the kro infra reflection acceptance kro instance CRD name (`demoinfrastructures.kro.run`) as a constant in `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Set the kro infra reflection acceptance script default `NAMESPACE` to `default` and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Set the kro infra reflection acceptance script default `CLUSTER_NAME` to `demo-cluster` and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Set the kro infra reflection acceptance script default `KRO_VERSION` to `0.7.1` and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Set the kro infra reflection acceptance script default `IMG` to `example.com/kany8s:acceptance-kro-infra` and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Made the kro infra reflection acceptance hack script accept `KIND_CLUSTER_NAME` (default `kany8s-acceptance-infra-${timestamp}`) and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Made the kro infra reflection acceptance hack script accept `KUBECTL_CONTEXT` (default `kind-${KIND_CLUSTER_NAME}`) and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Marked the infra kro reflection acceptance PRD item for `KRO_VERSION` env-var support complete; verified `make test` and `make lint`.

- 2026-02-03: Made the kro infra reflection acceptance hack script accept `CLEANUP` (default `true`) and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Made the kro infra reflection acceptance hack script accept `ARTIFACTS_DIR` (default `/tmp/kany8s-acceptance-kro-infra-${timestamp}`) and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Made the kro infra reflection acceptance hack script accept `KUBECONFIG_FILE` (default `${ARTIFACTS_DIR}/kubeconfig`) and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Marked the kro infra reflection acceptance PRD item for `IMG` env-var support complete (script already supported it + devtools assertion); verified `make test` and `make lint`.

- 2026-02-03: Made the kro infra reflection acceptance hack script accept `KRO_RGD_MANIFEST` (default `test/acceptance_test/manifests/kro/infra/rgd.yaml`) and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Marked the kro infra reflection acceptance PRD item for `NAMESPACE` env-var support complete (script already accepted it + devtools assertion); verified `make test` and `make lint`.

- 2026-02-03: Made the kro infra reflection acceptance hack script accept `KANY8S_CLUSTER_TEMPLATE` override (default `test/acceptance_test/manifests/kro/kany8scluster.yaml.tpl`) and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Added kro infra reflection acceptance `Kany8sCluster` manifest template at `test/acceptance_test/manifests/kro/kany8scluster.yaml.tpl` with required placeholders and a devtools test enforcing it; verified `make test` and `make lint`.

- 2026-02-03: Marked the kro infra reflection acceptance PRD item for `CLUSTER_NAME` env-var support complete; verified `make test` and `make lint`.

- 2026-02-03: Added `repo_root` resolution to `hack/acceptance-test-kro-infra-reflection.sh` (matching `hack/acceptance-test-kro-reflection.sh`) and asserted it in devtools tests; verified `make test` and `make lint`.

- 2026-02-03: Added `cd "${repo_root}"` to `hack/acceptance-test-kro-infra-reflection.sh` (run-from-anywhere safety) and extended the devtools assertion; verified `make test` and `make lint`.

- 2026-02-03: Made the kro infra reflection acceptance hack script accept `KRO_RBAC_WORKAROUND_MANIFEST` override (default `test/acceptance_test/manifests/kro/rbac-unrestricted.yaml`) and asserted it in devtools tests; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-03: Added a devtools test validating the kro infra acceptance RGD manifest parses as a Kubernetes object (YAML->unstructured; dry-run client parse equivalent); verified `make test` and `make lint`.

- 2026-02-03: Added a devtools test rendering the kro infra acceptance `Kany8sCluster` manifest template and validating it decodes into an unstructured Kubernetes object; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-03: Created the kro infra reflection acceptance artifacts dir (`mkdir -p "${ARTIFACTS_DIR}"`) and asserted it in the devtools script policy test; verified `make test` and `make lint`.

- 2026-02-03: Added kro infra reflection acceptance script logging (`${ARTIFACTS_DIR}/acceptance-infra.log`) via `tee` and extended devtools script policy assertions; verified `go test ./...` and `make lint`.

- 2026-02-03: Exported `KUBECONFIG` from `hack/acceptance-test-kro-infra-reflection.sh` (points at `${KUBECONFIG_FILE}`) and asserted it in devtools; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-03: Added kustomization backup/restore helpers to `hack/acceptance-test-kro-infra-reflection.sh` and asserted them in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `need_cmd()` helper to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added a `kubectl --context` wrapper (`k()`) to `hack/acceptance-test-kro-infra-reflection.sh` and extended the devtools script policy test; verified `make test` and `make lint`.

- 2026-02-03: Added a devtools assertion that `test/acceptance_test/manifests/kro/infra/` exists as a directory and marked the PRD item complete; verified `go test ./...` and `make lint`.

- 2026-02-03: Verified the kro infra acceptance RGD manifest `metadata.name` is `demo-infra.kro.run` and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-03: Ensured the rendered infra acceptance `Kany8sCluster` template contains no unreplaced placeholder tokens (devtools test); marked the PRD kubectl dry-run checklist item complete; verified `make test` and `make lint`.

- 2026-02-03: Implemented kro infra reflection acceptance `collect_diagnostics()` (kind/kubeconfig/kro/kany8s/Kany8sCluster dumps) and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `cleanup()` to `hack/acceptance-test-kro-infra-reflection.sh` (calls `restore_kustomization` first) and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added kind cluster deletion to infra reflection acceptance `cleanup()` when `CLEANUP=true` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added a keep-cluster message for infra reflection acceptance `cleanup()` when `CLEANUP=false` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `on_exit()` to `hack/acceptance-test-kro-infra-reflection.sh` to collect diagnostics on failure; extended devtools assertions; verified `make test` and `make lint`.

- 2026-02-03: Wired infra reflection acceptance `on_exit()` to call `cleanup` (preserve diagnostics then cleanup); updated devtools assertions; verified `make test` and `make lint`.

- 2026-02-03: Added `trap on_exit EXIT` to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `go test ./internal/devtools`, `bash -n hack/acceptance-test-kro-infra-reflection.sh`, and `make lint`.

- 2026-02-03: Added `need_cmd docker` preflight to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `need_cmd kind` preflight to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `go test ./...` and `make lint`.

- 2026-02-03: Added `need_cmd kubectl` preflight to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `need_cmd make` preflight to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `need_cmd go` preflight to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `need_cmd curl` preflight to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added kind cluster creation (`kind create cluster --wait 60s --kubeconfig ...`) to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Listed kind nodes right after cluster creation in `hack/acceptance-test-kro-infra-reflection.sh` and added a devtools assertion; verified `make test` and `make lint`.

- 2026-02-03: Ensured `kro-system` namespace exists in `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added infra reflection acceptance default `KRO_CORE_INSTALL_MANIFEST` path and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Ensured the kro infra reflection acceptance script creates the vendor cache dir for `KRO_CORE_INSTALL_MANIFEST`; updated devtools assertions; verified `make test` and `make lint`.

- 2026-02-03: Added kro install manifest download to `hack/acceptance-test-kro-infra-reflection.sh` (curl from GitHub releases when missing) and extended devtools assertions; verified `make test` and `make lint`.

- 2026-02-03: Added kro core install apply (`k apply -f "${KRO_CORE_INSTALL_MANIFEST}"`) to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `go test ./...` and `make lint`.

- 2026-02-03: Added kro rollout wait (`k -n kro-system rollout status deploy/kro --timeout=180s`) to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Applied the kro RBAC workaround manifest in `hack/acceptance-test-kro-infra-reflection.sh` (`k apply -f "${KRO_RBAC_WORKAROUND_MANIFEST}"`) and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Implemented infra RGD apply + wait in `hack/acceptance-test-kro-infra-reflection.sh` (`k apply -f "${KRO_RGD_MANIFEST}"` + `ResourceGraphAccepted` wait + CRD existence check) and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `make install` to `hack/acceptance-test-kro-infra-reflection.sh` (install Kany8s CRDs into the kind cluster) and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added `make docker-build IMG="${IMG}"` to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `go test ./internal/devtools` and `make lint`.

- 2026-02-03: Added `kind load docker-image "${IMG}" --name "${KIND_CLUSTER_NAME}"` to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Deployed the Kany8s controller-manager in `hack/acceptance-test-kro-infra-reflection.sh` (`backup_kustomization` + `make deploy` + rollout wait) and updated devtools assertions; verified `make test` and `make lint`.

- 2026-02-03: Rendered and applied the infra `Kany8sCluster` manifest from the template in `hack/acceptance-test-kro-infra-reflection.sh` and extended devtools assertions; verified `go test ./...` and `make lint`.

- 2026-02-03: Added a `Kany8sCluster` Ready wait (`kubectl wait --for=condition=Ready`) to `hack/acceptance-test-kro-infra-reflection.sh` and added a devtools repo-policy test; verified `make test` and `make lint`.

- 2026-02-03: Added a `Kany8sCluster` provisioned wait (`kubectl wait --for=jsonpath='{.status.initialization.provisioned}'=true`) to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added infra kro reflection acceptance checks to fail when `Kany8sCluster.status.failureReason/failureMessage` are set (jsonpath) and asserted them in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added a retry loop to `hack/acceptance-test-kro-infra-reflection.sh` to wait for the infra kro instance to exist (up to 240s) and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Added a `kubectl wait` for the infra kro instance `status.ready=true` to `hack/acceptance-test-kro-infra-reflection.sh` and asserted it in devtools; verified `make test` and `make lint`.

- 2026-02-03: Verified infra kro instance spec injection in `hack/acceptance-test-kro-infra-reflection.sh` (`.spec.clusterName/.spec.clusterNamespace`) and extended devtools coverage; verified `make test` and `make lint`.

- 2026-02-03: Made `hack/acceptance-test-kro-infra-reflection.sh` executable and added a devtools test enforcing it; verified `make test` and `make lint`.

- 2026-02-03: Added a devtools repo-policy test enforcing `bash -n` succeeds for `hack/acceptance-test-kro-infra-reflection.sh`; verified `go test ./...` and `make lint`.

- 2026-02-03: Added `test/acceptance_test/run-acceptance-kro-infra-reflection.sh` to `test/acceptance_test/README.md` and added a devtools test enforcing it; verified `go test ./...` and `make lint`.

- 2026-02-03: Added a Purpose line for the kro infra reflection runner in `test/acceptance_test/README.md` and enforced it via devtools; verified `make test` and `make lint`.

- 2026-02-03: Documented the kro infra reflection acceptance target in `docs/README.md` and added a devtools test enforcing it; verified `make test` and `make lint`.

- 2026-02-03: Documented the kro infra reflection acceptance target in `docs/e2e-and-acceptance-test.md` and added a devtools test enforcing it; verified `go test ./...` and `make lint`.

- 2026-02-03: Documented the kro infra reflection acceptance entry points in `docs/codebase.md` and added a devtools test enforcing it; verified `make test` and `make lint`.

- 2026-02-03: Added infra reflection execution to `test/acceptance_test/run-all.sh` with a devtools policy test; verified `go test ./internal/devtools` and `make lint`.

- 2026-02-03: Added `make test-acceptance-kro-infra-reflection` to `acceptance.md` and enforced it via a devtools test; verified `make test` and `make lint`.

- 2026-02-03: Added a devtools repo-policy test enforcing `bash -n` succeeds for `test/acceptance_test/run-acceptance-kro-infra-reflection.sh`; verified `go test ./...` and `make lint`.

- 2026-02-03: Marked the kro infra reflection acceptance PRD item for creating `hack/acceptance-test-kro-infra-reflection.sh` complete; verified `go test $(go list ./... | grep -v /e2e)` and `make lint`.

- 2026-02-03: Enforced the infra acceptance hack script shebang as the first line (devtools); verified `make test` and `make lint`.

- 2026-02-03: Enforced early `set -euo pipefail` strict-mode in the kro infra reflection acceptance hack script (devtools test) and marked the PRD item complete; verified `go test ./internal/devtools` and `make lint`.

- 2026-02-03: Enforced the infra reflection acceptance script `timestamp="$(date +%Y%m%d%H%M%S)"` definition via devtools and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-03: Enforced the infra reflection wrapper runner script executable bit via devtools and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-03: Marked the kro infra reflection acceptance PRD item for `KIND_CLUSTER_NAME` default complete; verified `go test ./...` and `make lint`.

- 2026-02-03: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `KUBECTL_CONTEXT` default; verified `make test` and `make lint`.

- 2026-02-03: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `KRO_VERSION` default; verified `make test` and `make lint`.

- 2026-02-03: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `IMG` default; verified `make test` and `make lint`.

- 2026-02-03: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `NAMESPACE` default; verified `make test` and `make lint`.

- 2026-02-03: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `CLUSTER_NAME` default; verified `make test` and `make lint`.

- 2026-02-03: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `CLEANUP` default; verified `make test` and `make lint`.

- 2026-02-04: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `ARTIFACTS_DIR` default; verified `make test` and `make lint`.

- 2026-02-04: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `KUBECONFIG_FILE` default; verified `make test` and `make lint`.

- 2026-02-04: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `RGD_NAME` constant; verified `make test` and `make lint`.

- 2026-02-04: Marked `docs/issues/kany8cluster-at-todo.md` item complete for infra reflection `RGD_INSTANCE_CRD` constant; verified `go test ./...` and `make lint`.

- 2026-02-04: Added a devtools ordering assertion for the infra reflection hack script `KRO_RBAC_WORKAROUND_MANIFEST` default (after repo_root cd) and marked the PRD item complete; verified `go test ./internal/devtools` and `make lint`.

- 2026-02-04: Marked infra reflection acceptance `KRO_RGD_MANIFEST` default PRD item complete; added a devtools ordering test to keep the default after `cd "${repo_root}"`; verified `make test` and `make lint`.

- 2026-02-04: Enforced infra reflection acceptance `KANY8S_CLUSTER_TEMPLATE` default ordering (after repo-root `cd`) via devtools and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-04: Marked the infra reflection acceptance wrapper runner PRD items complete and tightened the devtools wrapper script assertion to enforce `TIMESTAMP` override support; verified `make test` and `make lint`.

- 2026-02-04: Enforced `.PHONY` for kro infra reflection acceptance Makefile targets via devtools; marked the PRD Makefile target items complete; verified `make test` and `make lint`.

- 2026-02-04: Marked `docs/issues/kany8cluster-at-todo.md` repo-policy infra reflection "script exists" checklist complete (already enforced by devtools); verified `go test ./internal/devtools` and `make lint`.

- 2026-02-04: Marked the infra reflection repo-policy "Makefile target exists" checklist complete (already covered by devtools); verified `go test ./...` and `make lint`.

- 2026-02-04: Verified `go test ./internal/devtools` still passes and marked the corresponding checklist item complete in `docs/issues/kany8cluster-at-todo.md`; verified `make lint`.

- 2026-02-04: Verified `go test ./...` and `make lint` and marked the corresponding checklist item complete in `docs/issues/kany8cluster-at-todo.md`.

- 2026-02-04: Verified `bash -n hack/acceptance-test-kro-infra-reflection.sh` and marked the corresponding checklist item complete in `docs/issues/kany8cluster-at-todo.md`; verified `make test` and `make lint`.

- 2026-02-04: Verified `bash -n test/acceptance_test/run-acceptance-kro-infra-reflection.sh` and marked the corresponding checklist item complete in `docs/issues/kany8cluster-at-todo.md`; verified `make test` and `make lint`.

- 2026-02-04: Ran `make test-acceptance-kro-infra-reflection-keep` end-to-end on kind (CLEANUP=false) and marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-04: Captured observed `Kany8sCluster` YAML into acceptance artifacts (`${ARTIFACTS_DIR}/kany8scluster.observed.yaml`) in `hack/acceptance-test-kro-infra-reflection.sh` (supports manual provisioned verification); added a devtools assertion; verified `make test` and `make lint`.

- 2026-02-04: Captured observed infra kro instance YAML into acceptance artifacts (`${ARTIFACTS_DIR}/rgd-instance.observed.yaml`) in `hack/acceptance-test-kro-infra-reflection.sh` (supports manual `.spec.clusterName/.spec.clusterNamespace` verification); added a devtools assertion; verified `make test` and `make lint`.

- 2026-02-05: Added kro RGD schema helper `kro.SchemaHasSpecField` for `spec.schema.spec` opt-in checks (e.g., `clusterUID`), with unit tests; marked the PRD item complete; verified `make test` and `make lint`.

- 2026-02-05: Resolved owner CAPI `Cluster` in infrastructure `Kany8sCluster` kro mode (opt-in when RGD declares `clusterUID`) and surfaced `Ready=False`/`WaitingForOwnerCluster` requeues; added unit tests; verified `make test` and `make lint`.

- 2026-02-05: Injected owner `Cluster` UID into infrastructure kro instance `spec.clusterUID` only when the RGD schema declares it, and added unit tests covering both the inject/no-inject cases; verified `make test` and `make lint`.
