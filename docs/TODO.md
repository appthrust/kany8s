# TODO

- [ ] [P0] Implement CAPI v1beta2 InfrastructureCluster provisioned contract: add `status.initialization.provisioned` to `api/infrastructure/v1alpha1/kany8scluster_types.go`, set it in `internal/controller/infrastructure/kany8scluster_controller.go`, update `internal/controller/infrastructure/kany8scluster_reconciler_test.go` + `internal/devtools/kany8scluster_api_test.go`, run `make manifests generate test` (commit: `feat: implement InfrastructureCluster provisioned contract`).
- [ ] [P0] Scaffold infra template API via kubebuilder: `kubebuilder create api --group infrastructure --version v1alpha1 --kind Kany8sClusterTemplate --resource --controller=false` and commit the scaffold + generated artifacts (`PROJECT`, deepcopy, CRDs) (commit: `feat: scaffold infrastructure Kany8sClusterTemplate`).
- [ ] [P0] Align infra `Kany8sClusterTemplate` types to template conventions (ObjectMeta + Spec.KroSpec only; remove scaffold example fields) and add/extend devtools coverage in `internal/devtools/template_apis_test.go`, then `make manifests generate test` (commit: `feat: align infrastructure Kany8sClusterTemplate API`).
- [ ] [P0] Update `examples/capi/clusterclass.yaml` to reference the infra-group `Kany8sClusterTemplate` and adjust `internal/devtools/examples_capi_test.go`, then `make test` (commit: `fix: use infrastructure Kany8sClusterTemplate in ClusterClass example`).
- [ ] [P0] Deprecate the controlplane-group `Kany8sClusterTemplate` (keep temporarily, but stop documenting/using it): add a short note in `docs/design.md` and/or `docs/runbooks/clusterctl.md`, then `make test` (commit: `docs: deprecate controlplane Kany8sClusterTemplate`).
- [ ] [P0] Remove the controlplane-group `Kany8sClusterTemplate` after migration: delete `api/v1alpha1/kany8sclustertemplate_types.go`, remove the resource from `PROJECT` (via kubebuilder or carefully), update tests that reference it, run `make manifests generate test` (commit: `chore: remove controlplane Kany8sClusterTemplate`).
- [ ] [P1] Make invalid `Kany8sControlPlane.spec.kroSpec` non-fatal: catch JSON/unmarshal/type errors in `internal/controller/kany8scontrolplane_controller.go`, surface via Conditions + Event, add tests in `internal/controller/kany8scontrolplane_controller_test.go`, then `make test` (commit: `fix: surface invalid kroSpec via conditions`).
- [ ] [P1] Document `kroSpec` expectations (must be a JSON object because controller injects `spec.version`): update `docs/design.md` (optionally `docs/rgd-contract.md`), then `make test` (commit: `docs: clarify kroSpec must be an object`).
- [ ] [P1] Change failure semantics: stop setting `status.failureReason/failureMessage` during normal provisioning; reserve for terminal/controller-detected errors, update tests + `docs/rgd-contract.md`, then `make test` (commit: `fix: reserve failureReason for terminal errors`).
- [ ] [P2] Clarify RGD contract vs runtime behavior: update `docs/rgd-contract.md` to state fields are required for RGD authors but controller tolerates missing fields; point to `docs/rgd-guidelines.md`, then `make test` (commit: `docs: clarify RGD required fields vs runtime behavior`).
- [ ] [P2] Document kro dynamic RBAC rationale and a future tightening approach: add a section to `docs/design.md` (or new `docs/security.md`), then `make test` (commit: `docs: explain kro RBAC tradeoffs`).
- [ ] [P3] Clean up kubebuilder samples: replace `config/samples/*` TODO manifests with minimal working examples or redirect to `examples/`, then `make test` (commit: `docs: refresh samples to match examples`).
